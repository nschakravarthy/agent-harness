[project]
name = "stateful-agent-with-memory"
version = "0.1.0"
description = "Episodic plus semantic memory with compaction. A real conversation that survives context limits."
authors = [
    {name = "srini"}
]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.12,<4.0.0"
dependencies = [
    # Web layer
    "fastapi (>=0.136.1,<0.137.0)",
    "uvicorn[standard] (>=0.46.0,<0.47.0)",
    # Settings / validation
    "pydantic[email] (>=2.13.4,<3.0.0)",
    "pydantic-settings (>=2.14.1,<3.0.0)",
    # Persistence. asyncpg is the only driver: Postgres is the sole backend.
    # SQLAlchemy's async layer needs greenlet, which SQLAlchemy itself only
    # pulls in behind a marker listing aarch64 but not arm64, so Apple Silicon
    # misses it unless it is declared here.
    "sqlalchemy (>=2.0.49,<3.0.0)",
    "sqlmodel (>=0.0.38,<0.0.39)",
    "alembic (>=1.18.4,<2.0.0)",
    "asyncpg (>=0.30.0,<0.31.0)",
    "greenlet (>=3.0.0,<4.0.0)",
    # Auth
    "pyjwt (>=2.12.1,<3.0.0)",
    "argon2-cffi (>=23.1.0,<24.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pytest (>=9.0.3,<10.0.0)",
    "ruff (>=0.15.12,<0.16.0)",
    # Required by starlette's TestClient; not used by the app at runtime.
    "httpx (>=0.28.1,<0.29.0)"
]
